import { StyleSheet, Text, View, Image, Animated, ImageBackground, Button } from 'react-native';

export default function DarkModePage() {
    return (
        <View>
            <Text> Dark Mode Menu </Text>
        </View>
    )
}

const styles = StyleSheet.create({

});